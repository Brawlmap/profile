services:
  - type: web
    name: brawlmap
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: BRAWL_STARS_API_KEY
        sync: false
      - key: ALLOWED_ORIGIN
        sync: false